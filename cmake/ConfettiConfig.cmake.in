get_filename_component(Confetti_CMAKE_DIR "${CMAKE_CURRENT_LIST_FILE}" PATH)
include(CMakeFindDependencyMacro)

if(NOT TARGET Confetti::Confetti)
    include("${Confetti_CMAKE_DIR}/ConfettiTargets.cmake")
endif()

set(Confetti_LIBRARIES Confetti::Confetti)
